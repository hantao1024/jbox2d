<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.blofin.myjbox2d">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyJbox2d">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".widget.FirstActivity"></activity>
        <activity android:name=".two.TwoActivity"></activity>
        <activity android:name=".three.ThreeActivity"></activity>
        <activity android:name=".four.FourActivity"></activity>
        <activity android:name=".five.FiveActivity"></activity>
    </application>

</manifest>